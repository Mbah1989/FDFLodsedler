@page
@model FDFLodsedler.Pages.Leaders.VisLederModel
@{
}
<p>
    <a asp-page="/Leaders/OpretLeder"> Nyt Medlem</a>
</p>
<h1> Liste Af Ledre</h1>
<div class="card-deck">



    @foreach (var l in Model.Leders)
    {

        <div class="card-header">
            <h3>@l.Navn </h3>
            <div>
            </div>
            <div class="card-footer text-sm-center">
                <a asp-page="/Leaders/SletLeder" asp-route-sid="@l.Leder_Id" class="btn btn-danger m-1">Delete </a>

                <a asp-page="/Leaders/OpdatereLeder" asp-route-sid="@l.Leder_Id" class="btn btn-primary">Update </a>

            </div>

        </div>



    }

</div>
